{
  "name": "obx-conditions",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts",
    "check": "DB_PATH=/private/tmp/obx-conditions-check.sqlite bun run src/server.ts --once",
    "test": "bun test",
    "docker:push": "docker buildx build -f Dockerfile --platform=linux/amd64 -t ghcr.io/jwilson45/obx-conditions:0.1.0 -t ghcr.io/jwilson45/obx-conditions:latest --push ."
  }
}
